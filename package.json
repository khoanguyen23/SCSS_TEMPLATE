{
  "name": "scss-starter",
  "version": "1.0.0",
  "description": "Simple workflow for working with HTML and SCSS",
  "main": "index.js",
  "scripts": {
    "sass:build": "sass --no-source-map scss:css",
    "sass:watch": "sass --no-source-map --watch scss:css"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "sass": "^1.79.4"
  }
}
